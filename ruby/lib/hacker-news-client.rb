# frozen_string_literal: true

# Bundler default-require shim. The real entry point lives at
# `hacker/news/client` (matching the dashed gem name's strict-convention
# slashed file layout).
require_relative 'hacker/news/client'
